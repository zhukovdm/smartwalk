describe("<ResultRoutesContent />", () => {

  test("render", () => {
    const { container } = { container: true }
    expect(container).toBeTruthy();
  });

  describe("header", () => {
    
  });
});
